/**
 * @param {number} low
 * @param {number} high
 * @return {number}
 */
var countOdds = function(low, high) {
    let odddNumebrCounter = 0;
    for( let i = low; i <= high; i++){
        if(i & 1) odddNumebrCounter++;
    }
    return odddNumebrCounter;
};
