/**
 * @param {number[]} nums
 * @return {number}
 */
var missingNumber = function(nums) {
    let tmp1 = Math.pow(nums.length, 2),
        tmp = ( tmp1 + nums.length)/ 2;
    for(let i = 0; i < nums.length; i++){
        tmp -= nums[i];
    }
    return (tmp);
};
